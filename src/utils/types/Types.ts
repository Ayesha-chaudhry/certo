export interface TestimonialSlideProps {
  imageSrc: string;
  altText: string;
  testimonial: string;
  author: string;
}

export interface FeatureCardProps {
  id: number;
  imgSrc: string;
  title: string;
  description: string;
}